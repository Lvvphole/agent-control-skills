# Skill Dependency Graph

Acyclic order:

`execution-trigger-guard` -> `codex-conflict-safe-build` -> `contract-to-skills` -> `role-permission-guard` -> `artifact-manifest-governor` -> `idempotency-checkpointing` -> `state-gate-controller` -> `model-reality-boundary` -> `verifier-protocol` -> `evaluator-protocol` -> `release-gate` -> `memory-manager` -> `skill-lifecycle-governance`

`escalation-manager` may be invoked by any skill and does not grant downstream completion.
